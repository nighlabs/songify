# Claude Code Instructions

## Git Workflow

- Always create a feature/fix branch for changes. Never commit directly to main.
- Never force push. If a commit needs fixing, create a new commit instead.
- Use squash merges and not merge-commit or rebasing.